name: Park Imminent
markdown: kramdown
collections:
  recipes:
    output: true
  common-lisp:
    output: true
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
